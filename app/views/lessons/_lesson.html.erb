<li>
  <%= link_to gravatar_for(lesson.user, size: 60), lesson.user %>
  <span class="user"><%= link_to lesson.user.name, lesson.user %></span>
  <span class="content">Learned <%= lesson.results.count %> words in Lesson "<%= lesson.category.title %>" - (<%= lesson.created_at %>)</span>
</li>

