<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% unless current_user?(user) %>
    <%= link_to "", admin_user_path(user), method: :delete, data: {confirm: "Are you sure"}, class: "glyphicon glyphicon-trash" %>
  <% end %>
</li>
